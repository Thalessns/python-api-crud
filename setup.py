"""Setup info for the project."""
from setuptools import setup, find_packages

setup(
    name="Python API Crud",
    version="0.1.0",
    author="Thales",
    packages=find_packages(),
    python_requires=">=3.12",
    install_requires=[]
)
